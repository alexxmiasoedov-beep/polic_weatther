name: Collect weather market prices
# schedule re-registered 2026-08-27

# Каждые 30 минут весь день + каждые 10 минут в окне переворотов
# 19:00-22:00 UTC (22:00-01:00 Минска) — под поздние срезы владельца.
# Бюджет: ~60 запусков/день ~= 1800 мин/мес < 2000 бесплатных.
on:
  schedule:
    - cron: '5,35 * * * *'
    - cron: '15,25,45,55 19,20,21 * * *'
  workflow_dispatch:

concurrency:
  group: collect
  cancel-in-progress: false

jobs:
  collect:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Collect snapshot
        run: python collector.py

      - name: Commit and push
        run: |
          git config user.name "weather-collector"
          git config user.email "actions@users.noreply.github.com"
          git add data/
          if git diff --cached --quiet; then
            echo "nothing to commit"
            exit 0
          fi
          git commit -m "snapshot $(date -u +'%Y-%m-%d %H:%M') UTC"
          for i in 1 2 3 4; do
            git push && exit 0
            git pull --rebase
            sleep $((i * 2))
          done
          exit 1
